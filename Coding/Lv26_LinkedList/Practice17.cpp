#include <iostream>
#include <vector>
using namespace std;

// 배열의 업그레이판 vector라이브러리 사용

int main() {
    vector<int> v;

    v.push_back(1);
    v.push_back(2);
    v.push_back(5);
    v.push_back(7);

    return 0;
}